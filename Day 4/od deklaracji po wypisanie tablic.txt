            //deklaracja tablic
            //deklaracja iloœci elementów w tablicy tab
            int[] tab = new int[5];
            //przypisanie elementów tablicy tab1
            int[] tab1 = { 4, 2, 55, 4, 8 };
            //przypisanie elementów tablicy tab
            for (int i = 0; i < tab.GetLength(0); i++)
                tab[i] = i;
            //wypisanie elementów tablicy tab
            Console.Write("tab: ");
            for (int j = 0; j < tab.GetLength(0); j++)
                Console.Write(tab[j]+" ");
            //wypisanie elementów tablicy tab1
            Console.WriteLine();
            Console.Write("tab1: ");
            foreach (int l in tab1)
            {
                Console.Write(l+" ");
            }
            Console.ReadLine();